# --- Created by Slick DDL
# To stop Slick DDL generation, remove this comment and start using Evolutions

# --- !Ups
create table "User" ("id" BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL PRIMARY KEY,"firstname" VARCHAR NOT NULL,"lastname" VARCHAR NOT NULL,"email" VARCHAR NOT NULL,"password" VARCHAR NOT NULL,"dateJoined" BIGINT DEFAULT 1412221864101);
create unique index "idx_email" on "User" ("email");

